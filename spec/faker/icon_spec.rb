require 'spec_helper'

describe Faker::Icon do
  it 'has a version number' do
    expect(Faker::Icon::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
